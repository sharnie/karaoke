<div class="container aside-xl padder-v">
  <section class="m-b-lg">
    <header class="wrapper text-center">
      <strong>Edit your account</strong>
    </header>

    <%= devise_error_messages! %>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <div class="form-group">
        <%= f.text_field :full_name, class: "form-control rounded input-lg text-center no-border", placeholder: "Name" %>
      </div>
      <div class="form-group">
        <%= f.email_field :email, class: "form-control rounded input-lg text-center no-border", placeholder: "Email" %>
      </div>
      <div class="form-group">
        <%= f.password_field :password, class: "form-control rounded input-lg text-center no-border", placeholder: "Password" %>
      </div>

      <%= f.submit "Save changes", class: "btn btn-lg btn-warning lt b-white b-2x btn-block btn-rounded" %>

    <% end %>

  </section>
</div>